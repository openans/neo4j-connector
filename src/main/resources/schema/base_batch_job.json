{
  "description": "This has been created only because DevKit chokes on the additionalProperties of body -> data.json",
  "type": "object",
  "additionalProperties": false,
  "properties" : {
    "method": {
      "type": "string",
      "enum": ["GET", "POST", "PUT", "DELETE", "PATCH", "HEAD", "OPTIONS"]
    },
    "id": {
      "type": "integer"
    },
    "to": {
      "type": "string"
    }
  }
}
